package aQute.bnd.repo;

import junit.framework.*;

public class JPMRepoTest extends TestCase {

	public void testSimple() throws Exception {
		// RemoteLibraryImpl repo = new
		// RemoteLibraryImpl().url("http://jpm4j.org:8080/rest/");
		// Program p = repo.getProgram("biz.aQute.bndlib");
		// assertNotNull(p);
		// for (RevisionRef rr : p.revisions)
		// System.out.println(rr.version.base + " " + rr.master);
	}
}
